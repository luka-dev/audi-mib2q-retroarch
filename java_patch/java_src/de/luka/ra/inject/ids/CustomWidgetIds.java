package de.luka.ra.inject.ids;

/**
 * Widget IDs reserved for our injected items.
 *
 * Keep these stable so the same icon ID maps to the same visual asset.
 */
public final class CustomWidgetIds {
    private CustomWidgetIds() {
    }

    public static final int GAMES = 10;
}

